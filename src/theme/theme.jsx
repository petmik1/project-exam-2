import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#00679F',
    },
    secondary: {
      main: '#DAE6EF',
    },
  },
  container: {
    vh: '100vh',
  },
})

export default theme
